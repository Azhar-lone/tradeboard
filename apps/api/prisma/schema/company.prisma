model Company {
  id        Int        @id @default(autoincrement())
  name      String
  users     User[]     @relation("CompanyUsers")
  owners    User[]     @relation("CompanyOwners")
  items     Item[]
  movements Movement[]
  invoices  Invoice[]
  ledgers   Ledger[]
}
