model User {
  id        String      @id @default(cuid())
  email     String      @unique
  password  String       
  userName  String
  role      String 
  companyId String?
  company   Company?     @relation(fields: [companyId], references: [id])
  DsrReport DsrReport[]
}
