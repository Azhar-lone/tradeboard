model Item {
  id          Int       @id @default(autoincrement())
  sku         String    @unique
  name        String
  price       Float
  company     Company   @relation(fields: [companyId], references: [id])
  companyId   Int
  movements   Movement[]
}