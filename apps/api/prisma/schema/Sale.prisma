model Sale {
  id         String   @id @default(cuid())
  saleNo     String
  saleDate   DateTime
  customer   String

  companyId  String
  company    Company  @relation(fields: [companyId], references: [id])

  saleItems  SaleItem[]
}
