model Company {
  id       String   @id @default(cuid())
  name     String
  createdAt DateTime @default(now())

  users     User[]
  items     Item[]
  invoices  Invoice[]
  ledgers   Ledger[]
  stocks    Stock[]
}
