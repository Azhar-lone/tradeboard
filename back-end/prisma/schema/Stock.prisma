model Stock {
  id         String   @id @default(cuid())
  type       String   // current / damaged / expired / returned
  createdAt  DateTime @default(now())

  companyId  String
  company    Company  @relation(fields: [companyId], references: [id])

  stockItems StockItem[]
}
